# Reelseek (iOS)

Native SwiftUI iOS client for the Reelseek movie/TV streaming-availability app. Talks to the existing Cloud Run API (`https://movie-streaming-150176375922.europe-west1.run.app`) so all TMDb logic, sort rules, provider mappings, and IMDb enrichment stay in one place. The Cloud Run service kept its original name from before the rebrand — it's internal infrastructure.

## Requirements

- Xcode 15.3+
- iOS 17.0+ deployment target (uses SwiftData + `@Observable`)
- [XcodeGen](https://github.com/yonaskolb/XcodeGen) — to regenerate the `.xcodeproj` from `project.yml`

```sh
brew install xcodegen
```

## First-time setup

From the repo root:

```sh
cd ios
xcodegen generate
open Reelseek.xcodeproj
```

Then in Xcode: pick a Simulator (or your device), select the *Reelseek* scheme, ⌘R.

The Cloud Run base URL is baked into `Info.plist` (`APIBaseURL`). To point at a local Next.js dev server, edit that key in `Reelseek/Info.plist` before building — e.g. `http://localhost:3000` — and also flip `NSAllowsArbitraryLoads` to `true` for that build only.

## Project layout

```
ios/
├── project.yml                       # XcodeGen spec — single source of truth
└── Reelseek/
    ├── Info.plist                    # APIBaseURL, dark UI, portrait-only iPhone
    ├── App/                          # @main entry point + Theme
    ├── Models/                       # Codable mirrors of API DTOs
    ├── Networking/                   # APIClient (URLSession) + MovieAPI facade
    ├── ViewModels/                   # @Observable @MainActor view models
    ├── Persistence/                  # SwiftData @Model (WatchlistItem)
    ├── Views/                        # SwiftUI screens + reusable components
    └── Resources/Assets.xcassets/    # AppIcon, AccentColor, LaunchBackground
```

## Feature parity with the web app

| Web | iOS |
|---|---|
| Trending / home grid | `HomeView` |
| Advanced search form + sort-by | `SearchView` + `FiltersSheet` |
| Movie/TV detail page (cast, ratings, providers) | `DetailView` |
| Where-to-watch availability | provider cards on `DetailView` |
| Person search (autocomplete) | inline in `FiltersSheet` |
| — | Watchlist (SwiftData, iOS-only) |

The iOS app **does not** ship a TMDb or OMDb key. All title/genre/availability calls hit `/api/...` on Cloud Run.

## Regenerating the project after editing `project.yml`

```sh
cd ios
xcodegen generate
```

The `.xcodeproj` is gitignored — source of truth is `project.yml` + the Swift files.

## Notes on sort-by

The iOS app respects the same `SortKey` set as the web form (`popularity.desc`, `rating.desc/asc`, `release.desc/asc`, `votes.desc`). When sorting by rating, the API automatically applies a `vote_count.gte=200` floor — no client-side workaround needed.

## App Store submission checklist

### Done (in the repo)

- [x] App name: **Reelseek** (display name in `Info.plist` + project.yml).
- [x] Bundle ID: `com.khaledsamir.reelseek`.
- [x] 1024×1024 app icon, generated by `scripts/make_app_icon.swift` (re-run after edits).
- [x] About / attribution screen — `Reelseek/Views/AboutView.swift`, with required TMDb non-endorsement notice. Reached from the (i) button on the Home tab.
- [x] Privacy policy at `docs/privacy.md`, live at [ksmozn.github.io/MovieStreaming/privacy/](https://ksmozn.github.io/MovieStreaming/privacy/) via GitHub Pages.

### Still on you (one-time)

- [ ] **Enroll in the Apple Developer Program** ($99 / yr) at [developer.apple.com/programs/enroll](https://developer.apple.com/programs/enroll). Pick *Individual* — same-day approval, no D-U-N-S needed.
- [ ] **Reserve "Reelseek"** in App Store Connect → My Apps → +. Free until you upload a build. Fallbacks if taken: *Cuewatch*, *Whereon*, *Showbeat*, *Cinevo*, *FindReel*.
- [ ] **Email TMDb** at [api@themoviedb.org](mailto:api@themoviedb.org) to request commercial-use approval if you plan to monetize. (Required by their [API terms](https://www.themoviedb.org/about/api-terms). Reviewers do check.)
- [ ] **Screenshots** — 6.7" iPhone (iPhone 15/16 Pro Max) at 1290×2796. Capture from the Simulator with ⌘S once the app is running.
- [ ] **App Privacy questionnaire** in App Store Connect — answer "no" to most categories; declare the country-code preference as the only optional data.

### Recommended before first TestFlight

- [ ] Refine the app icon — current design is solid but a custom illustration from a designer would lift it further.
- [ ] Add a launch screen image — currently just a flat dark background.
- [ ] Smoke-test on a real iPhone, not just the Simulator (provider deep links open Safari, watchlist persists across launches).
